function Loading({isLoading}){
    return(
        <>
            <h1>Loading Status</h1>
            <p>{isLoading ? "....Loading":"hi this is an check which displays to you"}</p>
        </>
        
    )

}
export default Loading